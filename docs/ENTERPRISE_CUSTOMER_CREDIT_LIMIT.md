# 客户信用额度治理

`POST /api/enterprise/crm/customer-credit-limit` 根据应收、未开票订单、逾期、风险等级与授信证据决定额度调整。

- 自动计算预计信用暴露和调整后可用额度。
- 阻断制裁命中、严重逾期、高风险无审批和额度不足。
- 根据风险与金额生成信控或信用委员会审批路由。

返回 `APPROVE / REVIEW / BLOCKED`，所有判断均具备可评审的原因和补充动作。
